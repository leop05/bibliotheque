{% extends 'base.html.twig' %}

{% block title %}GenreLitteraire{% endblock %}

{% block body %}
    <h1>GenreLitteraire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre_litteraire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ genre_litteraire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_genre_litteraire_index') }}">back to list</a>

    <a href="{{ path('app_genre_litteraire_edit', {'id': genre_litteraire.id}) }}">edit</a>

    {{ include('genre_litteraire/_delete_form.html.twig') }}
{% endblock %}
